# 2026-07-15 工作记录

## 查询页优化：属性色/全量/排序/跨链接
- MoveSearch.vue：① 属性筛选 chip 改用图鉴属性色（chipStyle + 色点，沿用 TYPE_COLORS）；② 移除前80限制，列表显示全部；③ 排序优化（分类 physical/special→status，威力降序，中文名稳定排序）；④ 可学习宝可梦点击跳 `/pokemon/:id`；⑤ 支持 `?key=` 深链直接打开招式详情（供场地/状态页关联招式跳转）。
- AbilitySearch.vue：① 移除前80限制显示全部；② 按中文名排序；③ 拥有该特性的宝可梦点击跳 `/pokemon/:id`。
- CodexSearch.vue（天气/场地/异常状态共用）：相关特性标签可点跳 `/ability/:key`；新增「相关招式」区块，标签可点跳 `/tools/move-search?key=:key` 打开招式详情。
- 数据：TerrainInfo / StatusInfo 类型新增 `relatedMoves?: string[]`；pokemon-terrain.ts / pokemon-status.ts 补全 relatedMoves（均验证 key 存在于 MOVE_DB）。
- 校验：npm run build 通过 ✓。dev server 8082。

## 数据整理 + 地区分类 + 四类查询工具
- 新增数据类型（src/types/pokemon.ts）：ItemInfo / WeatherInfo / TerrainInfo / StatusInfo。
- 新建数据文件：
  - `src/data/pokemon-items.ts`：ITEM_DB（分类：精灵球/进化石/回复/战斗增强/携带道具/树果/秘传技能/重要道具，共 ~80 项，含 nameZh/category/descZh）。
  - `src/data/pokemon-weather.ts`：WEATHER_DB（无天气/晴朗/大日照/下雨/大雨/沙暴/冰雹/下雪/雾，含 relatedAbilities）。
  - `src/data/pokemon-terrain.ts`：TERRAIN_DB（无场地/电气/青草/薄雾/精神场地，含 relatedAbilities）。
  - `src/data/pokemon-status.ts`：STATUS_DB（中毒/剧毒/麻痹/灼伤/冰冻/睡眠/混乱/着迷/畏缩，含 color/relatedAbilities）。
- 世代→地区分类（constants/pokemon.ts）：
  - 原 `GENERATIONS` 改为 `REGIONS`（含 region/regionZh/gen/label/start/end），`GENERATIONS` 保留兼容导出。
  - VERSION_GROUPS 标签改为「关都/城都/丰缘/神奥/合众/卡洛斯/阿罗拉/伽勒尔/帕底亚地区」。
  - VERSION_ROWS 每条加 `region` 字段（宝可梦冠军=跨地区）。
  - Pokedex.vue 更多面板「世代」改为「地区」，显示「关都地区」并 range。
- 新增查询工具（小工具页）：
  - `src/views/tools/ItemSearch.vue`：道具搜索（分类筛选 + 详情）。
  - `src/views/tools/CodexSearch.vue`：通用组件（天气/场地/异常状态共用，含相关特性解析自 ABILITY_DB）。
  - `src/views/tools/WeatherSearch.vue` / `TerrainSearch.vue` / `StatusSearch.vue`：薄封装复用 CodexSearch。
  - tools.ts 注册 4 个工具（ready:true）；ToolDetail.vue 引入并 v-else-if 路由。
- 校验：npm run build 通过 ✓。

## 全局配置文件 + 赞助页重构
- 新建 `src/config/index.ts`：集中管理 App 通用配置 `APP_CONFIG`（name/version/author/email/dataSource + sponsor）。
  - `sponsor.enabled` 总开关；`sponsor.donate`{enabled,wechatQr,alipayQr,tip}；`sponsor.ad`{enabled,duration}。
  - 导出 interface AppConfig / SponsorConfig，含默认导出。
- 重构赞助页 `src/views/settings/HelpAuthor.vue`：改为读 APP_CONFIG 驱动。
  - 新增顶部导航（返回）+ 精灵球横幅（支持 xxx）。
  - 捐款/广告卡片各由 `sponsor.donate.enabled`/`sponsor.ad.enabled` 控制显隐；都关时显示兜底提示。
  - 二维码：配置了 wechatQr/alipayQr 则显示图片（.qr-img），否则内置示例 SVG 码。
  - 广告倒计时秒数、tip、作者署名均取自配置。
- 更新 `src/views/settings/Settings.vue`：引入 APP_CONFIG，应用名/版本/作者/数据来源/邮箱全部取配置；「帮助作者」入口用 `v-if="sponsorEnabled"` 控制；关于区新增「作者」行。
- 校验：npm run build 通过 ✓。

## 赞助页广告弹窗改造
- 广告从全屏 overlay 改为居中小弹窗卡片（`.ad-modal`，max-width 300px）。
- 内容改为占位提示：「嘻嘻没有广告」+「暂未接入激励视频，敬请期待」。
  - 底部两个按钮：「知道了」（关闭弹窗）+「领取奖励 ❤」（调 `onAdRewarded` → toast）。
- 预留 SDK 接口：
  - `openAd()` 函数内注释了 TODO 示例（穿山甲/AdMob 接入方式）。
  - `onAdRewarded()` 激励回调（当前手动触发，接入后由 SDK 回调调用）。
  - `onAdError()` 错误回调（注释保留，取消即可启用）。
  - `.ad-slot` 区域为广告位容器，后续 SDK 挂载点。
- 校验：npm run build 通过 ✓（仅既有大分包警告）。

## 努力值计算器（EvCalc）增强
- 需求：总点数达上限后不能再增加；进度条左右加「加满」「清空」按钮；一键加满超出总上限时拉到能分配的最大值。
- 实现（`src/views/tools/EvCalc.vue`）：
  - `clampEv(key, v)` 增加总上限约束：`maxByCap = totalCap - (usedTotal - evs[key])`，`min(hard, maxByCap)`。滑条与数值输入均调用它，总点数到上限后任何单项都无法再增加（不再只靠红字警告）。
  - 新增 `fillAll()`：按 STAT_ORDER 逐项 `Math.min(evMax, room)` 拉满，总点数到上限即停止；基础模式结果如 252/252/6/0/0/0，冠军模式 32/32/2/0/0/0。
  - 模板：进度条改为 `ev-summary-bar-row`，左侧「加满」按钮、右侧「清空」按钮（红/灰胶囊样式）。
- 校验：vue-tsc + npm run build 均通过 ✓（仅既有大分包警告）。

## 努力值计算：等级可输入修改
- 需求（用户「等级可以输入修改」）：基础模式下等级除了滑条也能直接输入数字。
- 实现（`src/views/tools/EvCalc.vue`）：
  - 新增 `onLevelInput(e)`：`raw===''` 时跳过，否则 `clamp(1,100,round(Number(raw)))` 写入 `level`。
  - 模板等级行基础模式分支：在 `van-slider v-model=level` 旁加 `<input type="number" :value="level" @input="onLevelInput">`（`.ev-level-input` 样式：宽 52、蓝字、1–100）。冠军模式仍锁定显示 `Lv.50`，无输入框。
- 校验：vue-tsc + npm run build 均通过 ✓。

## 打包 APK（全新环境从零搭建，已验证可复现）
本机原先**没有 Android SDK / 无 local.properties / 无 ANDROID_HOME**。最终成功产出 `android/app/build/outputs/apk/debug/app-debug.apk`（4.67MB，包名 com.example.androidvueapp，compileSdk 35）。关键步骤与坑：

1. **安装 Android SDK**（用 cmdline-tools + sdkmanager）：
   - 下载 `commandlinetools-win-..._latest.zip` 解压到 `C:\Users\28166\Android\Sdk\cmdline-tools\latest`。
   - `sdkmanager.bat --sdk_root=C:\Users\28166\Android\Sdk "platform-tools" "platforms;android-35" "build-tools;35.0.0"`（项目 variables.gradle 要求 compileSdk/targetSdk=35）。
2. **local.properties**：写 `android/local.properties`，`sdk.dir` 必须用**正斜杠** `C:/Users/28166/Android/Sdk`。用反斜杠 `C:\...` 时 AGP `SdkLocator.validateSdkPath` 会抛「文件名、目录名或卷标语法不正确」而失败。
3. **Gradle wrapper 下载超时**：`services.gradle.org` 在本机 10s 内连不上。解决：手动从腾讯镜像下载 `https://mirrors.cloud.tencent.com/gradle/gradle-8.11.1-all.zip`（约 220MB，~6s），放到 `~/.gradle/wrapper/dists/gradle-8.11.1-all/<hash>/gradle-8.11.1-all.zip`（hash 目录名 wrapper 首次运行会自建，如 `2qik7nd48slq1ooc2496ixf4i`），并删掉 `.part`/`.lck` 残留，重跑即跳过下载直接解包。
4. **JDK 版本**：capacitor-android 模块要求 **Java 21**。用 JDK 17 会报「无效的源发行版: 21」。本机 `C:\Program Files\Java\` 下已有 jdk-11/17/21，构建时 `JAVA_HOME` 设为 `C:\Program Files\Java\jdk-21`（Gradle 8.11 支持 JDK 21）。
5. **必须用 Bash（Git Bash）跑 gradlew.bat**，并显式设 Windows 风格路径：
   - `export JAVA_HOME='C:\Program Files\Java\jdk-21'`、`export ANDROID_HOME='C:\Users\28166\Android\Sdk'`、`export GRADLE_USER_HOME='C:\Users\28166\.gradle'`。
   - 关键：`GRADLE_USER_HOME` 必须显式设成 Windows 路径，否则默认 POSIX 路径 `/c/Users/...` 会让原生构建工具报路径语法错误。
   - **不要用 PowerShell 工具**：其 env 赋值会被工具桥接串改（JAVA_HOME 被拼上 `powershell.EXE`、行尾多 `X`），导致失败。
   - 命令：`./gradlew.bat assembleDebug --no-daemon`（首次会下载 AndroidX 依赖，约 1 分钟；成功显示 BUILD SUCCESSFUL）。
6. **产物**：`app/build/outputs/apk/debug/app-debug.apk`（debug 签名，未做 release 签名）。如需正式发布版需配 signingConfig（keystore）。

## 备注：遗留开发服务器
之前多次会话的后台 `npm run dev` 仍占用 8080–8084；本次未新建预览服务器（APK 构建不需要）。如需清理旧 dev 服务器可逐一结束。

## 离线图片打包 + 竖屏锁定（用户需求）
- 需求：安卓应用竖屏；完善本地图片缓存使离线可用。
- 实现：
  - `src/data/pokemon.ts` 的 `spriteFor(id)` 由硬编码 `https://raw.githubusercontent.com/.../{id}.png` 改为相对路径 `./sprites/${id}.png`。
  - 新增 `scripts/download-sprites.mjs`：用 Node 22 全局 fetch 并发(24)下载 PokeAPI 精灵图 1..1025 到 `public/sprites/`（共 3.1MB，失败 0），`xargs -P` 方式因「environment too large for exec」失败，故改用 Node 脚本。Vite 把 `public/` 拷进 `dist/`，`cap sync android` 再拷进 `android/app/src/main/assets/public/`；运行时相对路径在 file:// 下可访问，离线可见。各页面原有 `@error` 隐藏逻辑保留为兜底。
  - 竖屏：`android/app/src/main/AndroidManifest.xml` 的 `<activity>` 加 `android:screenOrientation="portrait"`。
- 重建 APK：因本机访问 `services.gradle.org` 超时，`./gradlew` 会卡在下载 gradle 发行包；直接调用已缓存 gradle 二进制绕过联网：`"C:/Users/28166/.gradle/wrapper/dists/gradle-8.11.1-all/2qik7nd48slq1ooc2496ixf4i/gradle-8.11.1/bin/gradle" assembleDebug`（JAVA_HOME=JDK21，GRADLE_USER_HOME=C:/Users/28166/.gradle）。产物 `app-debug.apk` 5.8MB，经 zipfile 校验含 1025 张 sprites（assets/public/sprites/）。

## 一键打包脚本（build-apk.sh）
- 用户需求：做一个可以一键打包的脚本。
- 新增 `build-apk.sh`（项目根目录，bash，Git Bash 下 `./build-apk.sh` 运行）：
  - 自动探测 managed Node 22（`C:/Users/28166/.workbuddy/binaries/node/versions/22.22.2`）并加入 PATH。
  - 自动探测 JDK 21（优先 `C:/Program Files/Java/jdk-21`，回退扫描 jdk-2x / JAVA_HOME），设 `JAVA_HOME` 与 `GRADLE_USER_HOME`。
  - 自动探测**本地缓存的 Gradle 二进制**（`find ~/.gradle/wrapper/dists -name gradle -path '*/bin/gradle'`），直接调用它而非 `./gradlew`，从而**绕过联网下载 Gradle 发行包**（本机网络访问不了 services.gradle.org）。
  - 流程：`npm run build` → `npx cap sync android` → `gradle assembleDebug`（或 `release` / `clean` 参数）→ 校验 APK 内含精灵图数量。
  - 若 `public/sprites` 精灵图 <1000 张，自动调用 `scripts/download-sprites.mjs` 重新下载（需联网；失败仅警告）。
  - 用法：`./build-apk.sh`（debug）、`./build-apk.sh release`、`./build-apk.sh clean`。
- 已后台运行验证中（重建 APK 以确认脚本端到端可用）。

## 双击打包脚本（build-apk.bat）
- 用户进一步要求：只要双击就能打包，不需要打开 Git Bash 等其他应用。
- 新增 `build-apk.bat`（项目根目录，Windows 原生批处理，资源管理器双击即可运行）。内容与 .sh 等价，但用 Windows 方式探测路径：
  - Node：`C:\Users\28166\.workbuddy\binaries\node\versions\22.22.2`（加入 PATH，调用 `npm.cmd`/`npx.cmd`）。
  - JDK21：`C:\Program Files\Java\jdk-21`（存在则用之；否则扫描 `jdk-2*`；再回退 `JAVA_HOME`），设 `JAVA_HOME` + `GRADLE_USER_HOME=%USERPROFILE%\.gradle`。
  - Gradle：`dir /s /b "%GRADLE_USER_HOME%\wrapper\dists\*gradle-8.11.1*\bin\gradle.bat"` 定位缓存二进制，直接调用绕过联网下载。
  - 精灵图 <1000 张时自动 `node scripts/download-sprites.mjs` 重下。
  - 流程：`npm run build` → `npx cap sync android` → `gradle -p android assembleDebug` → python 校验 APK 内含精灵图数。
  - 末尾 `pause >nul` 保留窗口，方便用户查看结果（双击场景）。
  - 用法：`build-apk.bat` / `build-apk.bat release` / `build-apk.bat clean`。
- 验证说明：沙箱 Bash/PowerShell 均禁止启动 cmd.exe，故无法在此自动跑 .bat；但已用 PowerShell 核验 .bat 依赖的全部路径（gradle.bat/jdk21/node/npm.cmd/npx.cmd/python/sprites=1025）均存在且匹配探测模式；且等效流水线已被 .sh 在本会话实测产出 5.8MB 含 1025 精灵图的 APK。

## 修复：双击 .bat 无法使用
- 现象：用户双击 build-apk.bat 报「无法使用」；且发现 build-apk.bat 与 build-apk.sh 均从磁盘消失（疑似被删/清理）。
- 根因（高概率）：原 .bat 由工具写入为 **LF 换行 + 无 BOM**，Windows cmd 解析批处理时可能直接失败、窗口一闪而过，表现为「无法使用」。
- 修复：重写 `build-apk.bat` 并用 Python 强制转为 **CRLF 换行 + 无 BOM**（验证 CRLF=157, bareLF=0, BOM=False）；同时加「日志包装」——首次运行把全部输出重定向到同目录 `build-apk.log` 再回显并 `pause`，保证即使窗口关闭也有完整错误日志可排查。重新创建已删的 `build-apk.sh`（LF，bash 用）。
- 待用户双击验证；若仍失败，让其把 `build-apk.log` 内容发我。

## 修复 build-apk.log 报错（用户双击实际报错）
用户发来 `build-apk.log`，暴露两个真 bug：
1. `[ERR] 未找到缓存的 Gradle 二进制`：原 Gradle 探测用 `dir /s /b "%GRADLE_USER_HOME%\wrapper\dists\*gradle-8.11.1*\bin\gradle.bat"`。cmd 的 `*` 不能跨目录分隔符 `\`，故匹配不到嵌套路径 `gradle-8.11.1-all\<hash>\gradle-8.11.1\bin\gradle.bat`。改为 `dir /s /b "...\dists\gradle.bat" | findstr /i "gradle-8.11.1"`（先按文件名递归找出所有 gradle.bat，再用 findstr 过滤版本），已确认能命中。
2. `'写到同目录' is not recognized`：原注释用 `::`，但 `::` 不是真正的注释（是 label 技巧），某些含中文/特殊字符的行会被当成命令执行。全部改为 `REM`（真正的注释命令，忽略特殊字符）。
- 重新写 `build-apk.bat` 并强制转 CRLF+无BOM（验证 CRLF=157, bareLF=0, 无 :: 注释残留）。Gradle 探测已用 PowerShell 等价逻辑 (`Get-ChildItem -Recurse -Filter gradle.bat | ? FullName -like *gradle-8.11.1*`) 确认能定位到缓存二进制。
- 待用户再次双击验证。

## 生成打包说明文档（用户需求）
- 用户要求生成一份 md 介绍，主要讲打包流程和使用的库。
- 新增 `打包说明.md`（项目根目录）：含七节——技术栈与使用的库（Vue3/Vite6/TS/Vue Router4/Pinia3/Vant4/Capacitor7 表格+离线设计）、环境要求、打包为静态网站、打包为安卓应用（Android Studio 与纯命令行两法）、关键配置速查、常见问题、构建脚本一览。

## 修复 一键打包.bat（双击报大量 'xxx' is not recognized）
- 现象：用户把 build-apk.bat 改名为 `一键打包.bat` 后双击，报 `'"JDK_HOME="' is not recognized`、`'gram'`、`'le_done'` 等几十行「不是内部或外部命令」。根因：与中文文件名无关，是**文件行尾符损坏**——被保存成 Unix LF（或混合），cmd 解析时整行错乱、按词拆分。
- 修复：用 Python 以 `utf-8 + CRLF + 无 BOM` 字节级重写 `一键打包.bat` 与 `build-apk.bat`（验证 CRLF=128, bareLF=0, BOM=False）。同时去掉易出错的「自我递归日志包装」(`call "%~f0" > log`)，改为直接运行 + 末尾 `pause` 保活窗口。Gradle 探测保持 `dir /s /b ...\gradle.bat | findstr "gradle-8.11.1"`（跨目录匹配，已验证命中缓存二进制）。
- 结论：双击 .bat 失败几乎都是编码/行尾问题（LF 而非 CRLF），用 Python 字节级重写可根治。

## cap open android 报错（本机无 Android Studio）
- 用户跑 `npm run build:android` 成功（vite build + cap sync 已同步，android/assets/public/sprites 含 1025 张），但 `npx cap open android` 报 `Unable to launch Android Studio. Is it installed?`——仅因本机未装 Android Studio，不影响已同步内容。
- 替代出 APK（已实测）：直接调缓存 Gradle 二进制跳过联网：`set JAVA_HOME=C:\Program Files\Java\jdk-21` 后 `"C:/Users/28166/.gradle/wrapper/dists/gradle-8.11.1-all/2qik7nd48slq1ooc2496ixf4i/gradle-8.11.1/bin/gradle" -p android assembleDebug` → 产物 `android/app/build/outputs/apk/debug/app-debug.apk`(5.8MB，zipfile 校验含 1025 精灵图)。`DetailNav.vue` 的 circular dependency warning 仅为警告非错误。

## 修复 一键打包.bat「尝试自动下载 was unexpected」(括号陷阱)
- 报错：`[OK] Gradle: ...` 之后 `尝试自动下载 was unexpected at this time.`（exit 255）。
- 根因：第 5 步精灵图检查的 `if (...) ( ... )` 块内，`echo` 文本含字面括号 `echo [WARN] 精灵图缺失 (%SPRITE_COUNT%/1025), 尝试自动下载`。批处理在**解析阶段**把 `(%SPRITE_COUNT%` 当作新块起始，导致后续中文被当成命令。注意：批处理整段 `if (...) (a) else (b)` 在解析时才报错，与运行时走哪个分支无关。
- 修复：去掉该 `echo` 及 else 分支 `echo [OK] 精灵图齐全 (%SPRITE_COUNT% 张)` 中的括号（改为 `%SPRITE_COUNT%/1025`、`%SPRITE_COUNT% 张`）。注意：顶层（不在 if 块内）的 `echo ...(npm run build)` 等带括号文本无碍，无需改。
- 用户要求清理：已删除 `build-apk.bat`、`build-apk.sh`、`build-apk.log`；仅保留修好的 `一键打包.bat`（Python 字节级 CRLF+无BOM 重写，验证 CRLF=128 bareLF=0 BOM=False）。`打包说明.md` 内两处 `build-apk.bat` 引用已改为 `一键打包.bat`。
- 提示：用户原有运行器指向 `build-apk.bat`，删后需改指 `一键打包.bat`。

## 应用改名 + 精灵球图标（用户需求）
- 用户要求：创建精灵球图标；修改名字为「宝可梦小图鉴」；后续打包全部沿用。
- **图标**：
  - 新增 `scripts/make-icon.py`（Pillow 程序化绘制扁平化精灵球）：红顶(#EF4444)/白底/黑腰带(#171717)/中心白按钮，1024px 高清源图 → LANCZOS 缩放。
  - 替换 Android 全部 mipmap 图标（5 密度 × 3 文件）：
    - `ic_launcher_foreground.png`：自适应前景（透明底，球占 ~92%），尺寸 108~432px。
    - `ic_launcher.png` / `ic_launcher_round.png`：白色圆角矩形背景 + 居中球，尺寸 48~192px。
  - 新增网站 favicon：`public/pokeball.svg`（SVG 版本，与 PIL 绘制一致）；`index.html` icon link 改为 `/pokeball.svg`。
  - 新增 `public/pokeball-512.png`（PNG 备用）。
- **改名**（四处同步）：
  - `capacitor.config.ts`：`appName: '宝可梦小图鉴'`（后续 cap sync 自动写入 strings.xml）。
  - `android/app/src/main/res/values/strings.xml`：`app_name` + `title_activity_main` 均改为「宝可梦小图鉴」。
  - `index.html`：`<title>宝可梦小图鉴</title>`。
- **重建 APK**：`npm run build` → `cap sync android`（确认 strings.xml 被正确覆盖为宝可梦小图鉴）→ Gradle `--no-daemon assembleDebug`（本机 AAPT2 daemon 启动需 Windows UCRT，加 --no-daemon 绕过）。产物 `app-debug.apk` **6.0MB**，zipfile 校验含 1025 张 sprites。
- **一键脚本更新**：`一键打包.bat` 的 Gradle 调用已加 `--no-daemon` 参数（本机必须）。
- **文档**：`打包说明.md` 已更新标题为「宝可梦小图鉴」，新增应用名/图标行，补充 favicon 说明。

## 设置页改版（用户需求）
- 删除「数据管理」section（含「补全图鉴数据」「清空图片缓存」两个下属功能）及其 JS 处理函数 `handleCompleteData`/`handleClearCache`。
- 隐藏「深色模式」行（remove darkMode ref 与对应模板/ CSS `row-icon--moon`）。
- 清理 `stores/settings.ts`：删除已无引用的 `clearImageCache`、`getMissingIds`（仅被上述功能使用）。
- 新增「帮助作者」入口：在「关于」section 内加可点击行 `goHelpAuthor()` → `router.push('/help-author')`；CSS 新增 `row-icon--heart`。
- 新增页面 `src/views/settings/HelpAuthor.vue`：DetailNav 返回；两个支持卡片（捐款 / 观看广告）。捐款=自定义弹窗（微信/支付宝切换 + 示例二维码占位 + 我已支付）；观看广告=模拟激励视频（5 秒倒计时后可领奖，未看完不可领）。`router/index.ts` 注册 `/help-author`（meta.tab='settings'）。
- 顺带把设置页两处硬编码「宝可梦图鉴」改为「宝可梦小图鉴」（与全局新名一致）。
- 验证：`npm run build`（vue-tsc + vite）EXIT=0，新增 HelpAuthor chunk(5.64kB)，Settings 块缩至 6.71kB。

